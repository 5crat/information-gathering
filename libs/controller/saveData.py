#!/usr/bin/env python
#-*-coding:utf-8-*-
#-*-author:scrat-*-


from libs.core.data import Queue,domainQueue,ipQueue

'''
def saveDataByQueue(db, dbtable, dataQueue=Queue()):
    while dataQueue:
        d = dataQueue.get()
        for i in d.keys():
'''